import React from 'react'

import * as classes from './Menu.module.scss'

const Menu = () => {
    return (
        <div className={classes.menu}>
            <div className={classes.menu_logo}>Todo App</div>
        </div>
    )
}

export default Menu
